import { ScrollItemStylePipe } from './scroll-item-style.pipe';

describe('ScrollItemStylePipe', () => {
  it('create an instance', () => {
    const pipe = new ScrollItemStylePipe();
    expect(pipe).toBeTruthy();
  });
});
